---
phase: 03-code-corrections-and-reproducible-qa-gates
plan: 06
type: execute
wave: 2
depends_on:
  - 03-05
files_modified:
  - backend/src/controller/equiposController.js
  - backend/tests/controllers/equiposController.test.js
autonomous: true
requirements:
  - MDL-13
user_setup: []
---

<objective>
Cerrar las brechas residuales de alcance en las lecturas de equipos que
aceptan `EQUIPOS.VIEW_OWN`: un Aprendiz solo puede consultar detalle,
verificaciones y movimientos de equipos con vínculo activo propio, mientras
que Administración, Instructor y Cuentadante conservan la visibilidad amplia
que les corresponde.
</objective>

<context>
El plan 03-05 corrigió `GET /:codigo/uso/historial`, pero la auditoría del
mismo controlador encontró que `obtenerEquipoPorCodigo`,
`obtenerHistorialEquipo` y `obtenerHistorialMovimientos` se montan con
`VIEW_OWN` y no validan el vínculo del Aprendiz. El arreglo debe ser fail-closed
y no enumerar equipos ajenos. No se debe inventar identidad, cambiar la
política de `registro-externo`, ni tocar evidencias físicas de aulas 303/304.
</context>

<tasks>
<task type="auto">
<name>Aplicar alcance común de lectura para Aprendiz</name>
<files>backend/src/controller/equiposController.js</files>
<action>Reutilizar el criterio de roles amplios y añadir validación segura de identidad y vínculo activo en Responsables_Equipo para detalle, historial de verificaciones e historial de movimientos. El equipo debe resolverse antes de devolver datos; un Aprendiz sin vínculo o identidad válida recibe 404 genérico y no obtiene historial ni responsables ajenos. Preservar filtros, consultas por código/placa donde existan, contratos de los roles amplios y filas externas sin ampliar la exposición.</action>
<verify>rg -n "obtenerEquipoPorCodigo|obtenerHistorialEquipo|obtenerHistorialMovimientos|Responsables_Equipo|EQUIPOS.VIEW_OWN" backend/src/controller/equiposController.js backend/src/routes/equiposRoutes.js</verify>
</task>
<task type="auto">
<name>Cubrir IDOR, privacidad y regresiones</name>
<files>backend/tests/controllers/equiposController.test.js</files>
<action>Añadir pruebas deterministas para Aprendiz con equipo asignado, equipo ajeno, identidad ausente y roles amplios en cada lectura afectada; comprobar que el historial no se consulta antes de autorizar y que el detalle no devuelve responsables ajenos. Mantener en verde las pruebas existentes de Cuentadante, Instructor y Administración.</action>
<verify>npm test --prefix backend -- --runInBand --forceExit --no-cache tests/controllers/equiposController.test.js tests/controllers/aprendicesController.test.js tests/controllers/importController.test.js</verify>
</task>
</tasks>

<success_criteria>
Ningún Aprendiz puede enumerar por código o placa un equipo no asignado ni
leer sus verificaciones/movimientos; un equipo asignado conserva el acceso
propio mínimo; los roles con `EQUIPOS.VIEW` no pierden su consulta amplia; las
tres suites focales y el lint de los archivos modificados quedan verdes.
</success_criteria>

<output>
Crear `03-06-SUMMARY.md` y `03-06-VERIFICATION.md` y un commit atómico con
referencia MDL-13. El worker no delega subagentes, no inicia otra Run, no
escribe en Linear y no hace push.
</output>
