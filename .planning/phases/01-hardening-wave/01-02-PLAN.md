---
phase: 01-hardening-wave
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/invitationCodeRoutes.js
  - backend/src/services/invitationCodeService.js
  - backend/src/middleware
  - backend/test
autonomous: true
requirements:
  - MDL-131
user_setup: []
must_haves:
  truths:
    - La validación pública de códigos de invitación está limitada por IP y por identificador disponible sin confiar únicamente en el cliente.
    - El consumo de un código comprueba y aumenta su uso dentro de una operación atómica/serializada.
    - La generación de códigos conserva entropía criptográfica suficiente y no expone valores predecibles.
    - Las pruebas cubren el límite y la carrera de dos consumos concurrentes.
  artifacts:
    - path: backend/src/routes/invitationCodeRoutes.js
      provides: Rate limit explícito para las rutas públicas de invitación.
    - path: backend/src/services/invitationCodeService.js
      provides: Consumo transaccional y seguro de códigos.
    - path: backend/test
      provides: Evidencia de abuso limitado y atomicidad.
  key_links:
    - from: public invitation validation route
      to: rate limiter
      via: middleware aplicado antes del servicio
    - from: useCode service
      to: database transaction
      via: row lock o actualización condicional equivalente
---

<objective>
Resolver MDL-131 endureciendo la superficie pública de invitaciones y eliminando la condición TOCTOU entre `validateCode` y `useCode`, sin cambiar innecesariamente el contrato de respuesta.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@backend/src/routes/invitationCodeRoutes.js
@backend/src/services/invitationCodeService.js
</execution_context>

<context>
La rama ya contiene la primera tanda de la PR #29. Inspecciona primero el ORM/driver y los patrones transaccionales existentes. Usa la primitiva que realmente soporte la base configurada (por ejemplo `SELECT ... FOR UPDATE` dentro de una transacción o un UPDATE condicional que compruebe el cupo); no simules atomicidad con un segundo read. El rate limit debe considerar las señales disponibles (IP y, cuando exista, documento/placa/código) sin registrar secretos en logs.
</context>

<tasks>
<task type="auto">
  <name>Revisar flujo y límites actuales</name>
  <files>backend/src/routes/invitationCodeRoutes.js, backend/src/services/invitationCodeService.js, backend/src/middleware, configuración y pruebas</files>
  <read_first>Rutas públicas de invitación, servicio, repositorio/driver de base de datos, middleware de rate limit y pruebas.</read_first>
  <action>Traza validación, consumo, generación, expiración y contador de usos. Documenta en el SUMMARY la primitiva transaccional disponible y el punto exacto donde ocurre la carrera; identifica límites globales ya existentes para no duplicar ni evadirlos.</action>
  <verify>rg -n "validate|useCode|invitation|rate|limit|transaction|FOR UPDATE|crypto|random" backend/src backend/test --glob '!node_modules/**'</verify>
  <acceptance_criteria>El diseño de la corrección se basa en las capacidades reales del driver y mantiene una respuesta API compatible.</acceptance_criteria>
  <done>Se conoce el contrato y el mecanismo de serialización antes de editar.</done>
</task>

<task type="auto">
  <name>Aplicar rate limit y consumo atómico</name>
  <files>backend/src/routes/invitationCodeRoutes.js, backend/src/services/invitationCodeService.js, backend/src/middleware y configuración estrictamente necesaria</files>
  <read_first>Patrones de middleware y transacciones descubiertos en la tarea anterior.</read_first>
  <action>Aplica el rate limit a la validación pública antes de llegar al servicio, con claves IP/documento/placa cuando estén disponibles y respuestas coherentes. Reescribe `useCode` para que verifique expiración/cupo y actualice el contador dentro de una transacción con bloqueo de fila o UPDATE condicional equivalente. Si la generación es débil, usa el módulo criptográfico apropiado del runtime; conserva formato solo si no reduce la entropía. No toques rutas ajenas.</action>
  <verify>git diff --check; revisión estática de que no hay read-then-write fuera de la transacción; comprobación de que los logs no incluyen códigos completos.</verify>
  <acceptance_criteria>Dos consumos concurrentes no pueden superar el cupo y la ruta pública no puede ser sondeada sin límite efectivo.</acceptance_criteria>
  <done>El diff contiene únicamente la defensa de invitaciones y su configuración/pruebas.</done>
</task>

<task type="auto">
  <name>Probar y entregar MDL-131</name>
  <files>backend/test</files>
  <read_first>Scripts y fixtures del backend; pruebas de rutas, servicios y base de datos existentes.</read_first>
  <action>Añade pruebas del rate limit, expiración/cupo y carrera de consumo usando las herramientas ya adoptadas por el repo. Ejecuta suites focalizadas y documenta limitaciones de infraestructura si una prueba de concurrencia necesita una base no disponible. Crea `.planning/phases/01-hardening-wave/01-02-SUMMARY.md`, haz un commit atómico con `MDL-131` y envía `worker_done` con hash y resultados.</action>
  <verify>Pruebas focalizadas de invitation routes/service/middleware; `git diff --check`; revisión del diff contra HEAD base.</verify>
  <acceptance_criteria>La evidencia demuestra el límite y la atomicidad, o marca BLOCKED con la dependencia concreta y sin afirmar una garantía no probada.</acceptance_criteria>
  <done>SUMMARY creado, commit atómico realizado y worker_done enviado.</done>
</task>
</tasks>

<verification>
Ejecutar las pruebas focalizadas del servicio y rutas de invitación, incluyendo el caso concurrente; revisar diff, logs y ausencia de secretos; ejecutar `git diff --check`.
</verification>

<success_criteria>
MDL-131 queda protegido contra abuso y sobreconsumo concurrente con evidencia local reproducible, o queda explícitamente BLOCKED con causa técnica verificable.
</success_criteria>

<output>
Crear `.planning/phases/01-hardening-wave/01-02-SUMMARY.md` y un commit atómico con referencia `MDL-131`.
</output>

