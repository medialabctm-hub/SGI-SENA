---
phase: 01-hardening-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/config/api.js
  - frontend/src/components/ProtectedRoute.jsx
  - backend/src
  - frontend/src
  - backend/test
  - frontend/src/**/*.test.*
autonomous: true
requirements:
  - MDL-127
user_setup: []
must_haves:
  truths:
    - El navegador no persiste JWT de autenticación en localStorage o sessionStorage.
    - El backend emite y revoca la sesión mediante cookies httpOnly con atributos Secure/SameSite coherentes con el entorno.
    - El frontend envía credenciales y las guardas de ruta reflejan el estado de sesión sin leer secretos del navegador.
    - Las pruebas o comprobaciones focalizadas dejan evidencia del contrato y de las rutas críticas.
  artifacts:
    - path: frontend/src/config/api.js
      provides: Cliente HTTP/configuración de credenciales sin persistencia de JWT.
    - path: frontend/src/components/ProtectedRoute.jsx
      provides: Guardia de rutas basada en sesión y no en tokens expuestos.
    - path: backend/src
      provides: Emisión, lectura y revocación de cookies de sesión en los endpoints existentes.
  key_links:
    - from: backend auth response
      to: frontend API client
      via: Set-Cookie y solicitudes con credentials incluidos
    - from: frontend route guard
      to: backend session verification
      via: endpoint de sesión existente o equivalente documentado
---

<objective>
Implementar el alcance seguro de MDL-127 sobre la arquitectura de autenticación existente, eliminando la exposición de JWT en almacenamiento web y manteniendo funcionales login, logout y protección de rutas.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@frontend/src/config/api.js
@frontend/src/components/ProtectedRoute.jsx
</execution_context>

<context>
La rama parte del HEAD de la PR #29 y contiene cambios previos de cierre. Lee primero las rutas/controladores de login, el servicio JWT, configuración de cookies/CORS y las pruebas actuales antes de editar. Conserva el contrato funcional de la aplicación y no inventes un proveedor de identidad ni un mecanismo de refresh incompatible con el backend actual. Si la aceptación completa requiere una decisión de arquitectura que no puede resolverse de forma segura en este plan, reporta BLOCKED con evidencia y deja únicamente un cambio acotado y verificable.
</context>

<tasks>
<task type="auto">
  <name>Mapear el contrato actual de autenticación</name>
  <files>backend/src, frontend/src/config/api.js, frontend/src/components/ProtectedRoute.jsx, pruebas de auth</files>
  <read_first>Rutas/controladores de login/logout, JwtService, configuración de CORS/cookies, cliente API, ProtectedRoute y pruebas existentes.</read_first>
  <action>Identifica dónde se firma, transporta, almacena, verifica y revoca el JWT. Define en el propio cambio o en una nota de resumen el contrato mínimo de cookie (nombre, httpOnly, Secure, SameSite, path, expiración) y los distintos valores de desarrollo/producción. No modifiques archivos no relacionados para ocultar fallos de pruebas.</action>
  <verify>rg -n "localStorage|sessionStorage|jwt|Set-Cookie|cookie|credentials" frontend/src backend/src backend/test frontend/src --glob '!node_modules/**'</verify>
  <acceptance_criteria>Existe un mapa concreto de los puntos de emisión/consumo y una estrategia compatible con el código actual antes de la implementación.</acceptance_criteria>
  <done>El resumen identifica los archivos tocados y el contrato de sesión elegido.</done>
</task>

<task type="auto">
  <name>Migrar el transporte de sesión y las guardas</name>
  <files>backend/src, frontend/src/config/api.js, frontend/src/components/ProtectedRoute.jsx</files>
  <read_first>El mapa producido en la tarea anterior y los patrones de middleware/configuración ya usados por el repositorio.</read_first>
  <action>Implementa emisión y limpieza de cookie httpOnly en los endpoints de autenticación existentes; configura atributos seguros sin romper desarrollo local; ajusta CORS/credentials y el cliente frontend; elimina lecturas/escrituras de JWT en localStorage/sessionStorage y adapta ProtectedRoute al mecanismo de sesión. Mantén mensajes y respuestas compatibles donde no contradigan MDL-127.</action>
  <verify>rg -n "localStorage\.(setItem|getItem|removeItem)|sessionStorage\.(setItem|getItem|removeItem)" frontend/src backend/src --glob '!node_modules/**'; git diff --check</verify>
  <acceptance_criteria>No queda un JWT de aplicación en almacenamiento web; login/logout y la verificación de sesión usan cookies httpOnly y la configuración de credenciales es explícita.</acceptance_criteria>
  <done>El diff queda limitado al contrato de autenticación y no contiene secretos.</done>
</task>

<task type="auto">
  <name>Probar y entregar MDL-127</name>
  <files>backend/test, frontend/src/**/*.test.*, documentación estrictamente necesaria</files>
  <read_first>Scripts de prueba del backend/frontend y pruebas de autenticación existentes.</read_first>
  <action>Añade o actualiza pruebas focalizadas para cookie, logout, credenciales y protección de rutas. Ejecuta los comandos locales apropiados del repositorio, además de una búsqueda estática de persistencia de tokens. No afirmes UAT de navegador o producción si no fue ejecutado. Crea un SUMMARY GSD en `.planning/phases/01-hardening-wave/01-01-SUMMARY.md` con cambios, comandos, resultados y gaps; realiza un commit atómico cuyo mensaje incluya `MDL-127` y envía `worker_done` con el hash.</action>
  <verify>Pruebas focalizadas del backend y frontend; `git diff --check`; búsqueda sin coincidencias de persistencia de JWT salvo mocks/documentación justificada.</verify>
  <acceptance_criteria>La evidencia distingue PASS, FAIL y BLOCKED, y el commit es reproducible desde la rama base de PR #29.</acceptance_criteria>
  <done>SUMMARY creado, commit atómico realizado y worker_done enviado.</done>
</task>
</tasks>

<verification>
Ejecutar las suites focalizadas de autenticación de backend y frontend definidas por sus scripts; comprobar que no existe almacenamiento de JWT; ejecutar `git diff --check` y revisar el diff completo contra el HEAD base.
</verification>

<success_criteria>
MDL-127 queda implementado con cookies httpOnly y evidencia local suficiente, o queda explícitamente BLOCKED con el punto técnico exacto que requiere decisión humana.
</success_criteria>

<output>
Crear `.planning/phases/01-hardening-wave/01-01-SUMMARY.md` y un commit atómico con referencia `MDL-127`.
</output>

