---
phase: 01-hardening-wave
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - start.sh
  - Documentation/DEPLOY_RAILWAY.md
  - backend/src
  - backend/test
autonomous: true
requirements:
  - MDL-134
user_setup: []
must_haves:
  truths:
    - El `PORT` que Railway inyecta no se sobrescribe con el puerto interno del backend.
    - Nginx y backend tienen variables/puertos internos explícitos y el arranque es idempotente.
    - `Documentation/DEPLOY_RAILWAY.md` describe el contrato real de variables y el flujo de arranque.
    - La revisión de `finalizarClase` y su procedimiento almacenado deja una prueba o una brecha explícita sobre idempotencia.
  artifacts:
    - path: start.sh
      provides: Arranque Railway con separación entre PORT externo y puertos internos.
    - path: Documentation/DEPLOY_RAILWAY.md
      provides: Contrato documentado de despliegue y puertos.
    - path: backend/src
      provides: Corrección acotada solo si la revisión de finalización demuestra una regresión reproducible.
  key_links:
    - from: Railway PORT
      to: nginx listen/proxy
      via: NGINX_PORT derivado sin reescribir PORT
    - from: finalizarClase
      to: stored procedure/database write
      via: guardia de idempotencia revisada con evidencia
---

<objective>
Cerrar MDL-134 con un contrato de arranque compatible con Railway y una revisión reproducible de la idempotencia de `finalizarClase`, manteniendo el cambio pequeño y verificable.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@start.sh
@Documentation/DEPLOY_RAILWAY.md
</execution_context>

<context>
La aceptación conocida exige que `start.sh` no convierta el `PORT` público de Railway en `3000` mientras arranca Nginx/backend. Revisa además la implementación actual de `finalizarClase` y el procedimiento almacenado antes de tocarla: si ya es idempotente, añade evidencia/documentación y no hagas una refactorización oportunista; si existe una carrera o doble cierre demostrable, aplica el parche mínimo y añade una prueba focalizada. No ejecutes un despliegue real ni afirmes UAT de Railway desde este worker.
</context>

<tasks>
<task type="auto">
  <name>Auditar el contrato de arranque y cierre</name>
  <files>start.sh, Documentation/DEPLOY_RAILWAY.md, backend/src, SQL/procedures y pruebas relacionadas</files>
  <read_first>Script de arranque completo, configuración Nginx/Docker, documentación Railway, controlador/servicio de finalizarClase y procedimiento almacenado.</read_first>
  <action>Traza el origen y uso de `PORT`, `BACKEND_PORT` y `NGINX_PORT`; contrasta la documentación con el script. Revisa las guardias de estado/constraints/transacción de `finalizarClase` y registra si la idempotencia está probada o solo asumida. No cambies código durante este diagnóstico salvo que una prueba de lectura requiera un fixture.</action>
  <verify>rg -n "PORT|BACKEND_PORT|NGINX_PORT|finalizarClase|finalizar_clase|CALL|UPDATE" start.sh Documentation backend --glob '!node_modules/**'</verify>
  <acceptance_criteria>El SUMMARY puede explicar con líneas concretas qué variable es externa, cuáles son internas y cuál es la garantía real del cierre.</acceptance_criteria>
  <done>Se identifican discrepancias y el parche mínimo antes de editar.</done>
</task>

<task type="auto">
  <name>Corregir puertos y documentar el contrato</name>
  <files>start.sh, Documentation/DEPLOY_RAILWAY.md, configuración estrictamente necesaria</files>
  <read_first>El contrato auditado y el estilo de documentación existente.</read_first>
  <action>Haz que `PORT` permanezca reservado al listener público de Railway y define defaults explícitos para `BACKEND_PORT` y `NGINX_PORT` sin sobrescritura circular. Ajusta la configuración que consume esas variables si es necesario y documenta variables, defaults, healthcheck y modo local en `Documentation/DEPLOY_RAILWAY.md`. Mantén el script seguro ante ejecuciones repetidas y no guardes secretos en documentación.</action>
  <verify>Revisión estática del shell con el checker disponible; pruebas de expansión/contrato si existen; `git diff --check`; comprobar que no aparece `export PORT=3000` ni equivalente que pise el valor externo.</verify>
  <acceptance_criteria>El script preserva el `PORT` externo y el documento permite reproducir el arranque con valores explícitos.</acceptance_criteria>
  <done>El contrato queda implementado y documentado, sin tocar archivos no relacionados.</done>
</task>

<task type="auto">
  <name>Validar idempotencia y entregar MDL-134</name>
  <files>backend/src, backend/test, SQL/procedures solo si el análisis demuestra un bug</files>
  <read_first>Pruebas del backend y evidencia obtenida en las tareas anteriores.</read_first>
  <action>Ejecuta las pruebas focalizadas del arranque y de finalización. Si `finalizarClase` ya impide doble aplicación, documenta la evidencia; si no, implementa la mínima guardia transaccional/condicional y cubre reintento/doble llamada. Crea `.planning/phases/01-hardening-wave/01-03-SUMMARY.md`, haz un commit atómico con `MDL-134` y envía `worker_done` con hash, comandos y cualquier gap de UAT.</action>
  <verify>Pruebas focalizadas disponibles, checker de shell, `git diff --check` y revisión completa del diff.</verify>
  <acceptance_criteria>Queda demostrada la preservación del `PORT` y queda separada la evidencia local de cualquier validación que requiera Railway real.</acceptance_criteria>
  <done>SUMMARY creado, commit atómico realizado y worker_done enviado.</done>
</task>
</tasks>

<verification>
Ejecutar checks de shell/configuración, pruebas focalizadas del cierre y revisar el documento contra `start.sh`. Registrar explícitamente que no se realizó UAT de Railway salvo evidencia real.
</verification>

<success_criteria>
MDL-134 queda corregido y documentado con un contrato de puertos no destructivo y una conclusión basada en evidencia sobre la idempotencia de finalización.
</success_criteria>

<output>
Crear `.planning/phases/01-hardening-wave/01-03-SUMMARY.md` y un commit atómico con referencia `MDL-134`.
</output>

